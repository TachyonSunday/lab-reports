% !TEX root = ../lab2_report.tex
% ═══════════════════════════════════════════════════════════════
% 实验二：叶轮机非定常压力参数测量与数据处理
% ═══════════════════════════════════════════════════════════════

\section{实验二：叶轮机非定常压力参数测量与数据处理}

\subsection{实验目的}

\begin{enumerate}
    \item 应用动态压力传感器测量轴流式叶轮机械转子顶部间隙端壁处压力脉动。
    \item 观察不同转速下由于转子叶片与压气机端壁相干作用产生的流场周期性变化。
    \item 分析压力脉动周期与转速、叶片数的定量关系。
    \item 学会使用 Python/Matlab 进行傅里叶变换（FFT）和数据处理绘图。
\end{enumerate}

\subsection{实验原理}

在轴流压气机运行过程中，转子叶尖与机匣端壁之间存在相对运动和间隙流动（叶尖泄漏流）。叶片周期性扫过机匣壁面静压测点时，产生周期性的压力脉动信号。通过高频响应的动态压力传感器（Kulite）采集这些信号，再经过快速傅里叶变换（FFT）将其从时域转换到频域，可以识别出叶片通过频率（BPF）及其谐波成分。BPF的计算公式为：

\begin{equation}
    \text{BPF}_{\text{R1}} = \frac{n_1}{60} \times N_{\text{R1}}, \quad
    \text{BPF}_{\text{R2}} = \frac{n_2}{60} \times N_{\text{R2}}
\end{equation}

式中 $n_1, n_2$ 为上、下游转子转速（rpm），$N_{\text{R1}}, N_{\text{R2}}$ 为上、下游转子叶片数。

\subsection{实验装置}

\begin{itemize}
    \item 低速轴流对转压气机实验台（同实验一）
    \item Kulite 高频响应动态压力传感器 3 支
    \item 动态信号采集系统（A/D转换，5.12 kHz采样）
    \item 信号放大器与低通滤波器
    \item 节流锥控制系统
\end{itemize}

本实验实际使用的传感器布置与指导书原始描述有所不同。3 支动态压力传感器安装于下游转子进口截面的机匣壁面，沿周向布置。具体而言，两支传感器分别位于相邻两片下游转子叶片前缘的前方，第三支位于两叶片前缘连线中点（即1/2栅距处）的前方。

传感器周向间距为一个栅距（32.5 mm），其目的在于考察转子叶片通过时产生的周期性压力脉动沿周向的传播特征。相邻叶片前缘处的两个信号具有确定的相位差（对应一个叶片通过周期），中点处的信号则反映两个压力脉动源的叠加效应。

\subsection{实验步骤}

\begin{enumerate}
    \item 启动动态测量程序，设置参数文件（采样频率、触发方式等）。
    \item 设置采样频率为 5.12 kHz，存储方式为"手动触发+定时触发(10 s)"。
    \item 启动节流锥控制程序，设置节流锥位置。
    \item 开启前后变频器至目标转速。
    \item 依次点击"采集""触发"按钮，采集10秒数据。
    \item 通过"分析→输出"导出数据为文本文档。
    \item 切换节流锥位置和转速，重复测量。
    \item 实验结束：退回节流锥，停止变频器，关闭所有程序。
\end{enumerate}

\subsection{数据处理方法}

\subsubsection{叶片通过频率 (BPF)}

变频器频率 $f_{\text{VFD}}$（Hz）与电机转速 $n$（rpm）的关系为 $n = f_{\text{VFD}} \times 60$。叶片通过频率：

\begin{equation}
    \text{BPF}_{\text{R1}} = \frac{n_1}{60} \times N_{\text{R1}} = f_{\text{VFD,1}} \times 12, \quad
    \text{BPF}_{\text{R2}} = \frac{n_2}{60} \times N_{\text{R2}} = f_{\text{VFD,2}} \times 10
\end{equation}

本实验两组转速条件下的 BPF：
\begin{itemize}
    \item 前30 Hz/后35 Hz：$\text{BPF}_{\text{R1}} = 30 \times 12 = 360$ Hz，$\text{BPF}_{\text{R2}} = 35 \times 10 = 350$ Hz
    \item 前28 Hz/后37 Hz：$\text{BPF}_{\text{R1}} = 28 \times 12 = 336$ Hz，$\text{BPF}_{\text{R2}} = 37 \times 10 = 370$ Hz
\end{itemize}

\subsubsection{傅里叶变换 (FFT)}

离散傅里叶变换公式：
\begin{equation}
    X(k) = \sum_{n=0}^{N-1} x(n) \cdot e^{-j2\pi k n / N}, \quad k = 0, 1, \dots, N-1
\end{equation}

取单边幅值谱（正频率部分）：
\begin{equation}
    |X(k)|_{\text{one-sided}} = \frac{2|X(k)|}{N}, \quad k = 1, 2, \dots, \frac{N}{2}-1
\end{equation}

频率分辨率 $\Delta f = f_s / N$，其中 $f_s = 5120$ Hz 为采样频率，每组数据采集约 $N = 128512$ 点（约25 s），频率分辨率约 0.04 Hz。

\subsection{数据处理结果及分析}

实验在四组工况下进行，分别为两种转速组合（前30 Hz后35 Hz、前28 Hz后37 Hz）在两种节流状态（全开90\textdegree{}和近失速15\textdegree{}）下的测量。原始数据文件命名格式为 \texttt{08-前频率-后频率-节流角.txt}。

\subsubsection{信号统计量}

\input{tables/table_dynamic_stats.tex}

表~\ref{tab:dynamic_stats} 列出了四组工况下三个测点的压力信号统计量。可以观察到：
\begin{enumerate}
    \item 近失速工况（15\textdegree{}）下压力脉动的 RMS 和峰峰值远大于全开工况（90\textdegree{}）。以30/35 Hz组为例，探针1的RMS从179 Pa 增大到373 Pa，峰峰值从540 Pa 增大到3321 Pa，增幅分别达108\% 和515\%。
    \item 两种转速组合在相同节流角下的脉动统计量接近，表明脉动强度主要受节流状态（流量）影响，而转速组合差异的影响相对较小。
    \item 全开工况下三个探针的均值均为负值（$-120\sim-200$ Pa），而近失速工况下为正值（$170\sim260$ Pa），反映了两种状态下下游转子进口截面压力水平的差异。
\end{enumerate}

\subsubsection{时域信号}

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{figures/exp2_dynamic_time_domain.png}
    \caption{四组工况下动态压力时域信号对比（前30 ms）}
    \label{fig:time}
\end{figure}

图~\ref{fig:time} 展示了四组工况下前 30 ms 的时域信号。可以观察到：
\begin{enumerate}
    \item 近失速工况（15\textdegree{}）下信号幅值显著大于全开工况（90\textdegree{}），三种探针的波形高度一致，表明周向布置的三个测点都感受到了相似的周期性压力脉动。
    \item 全开工况下信号幅值较小、波形较为平坦，表明此时下游转子进口的流动较为均匀，叶片与端壁的相干作用较弱。
    \item 两转速组合的波形特征相似，说明转速在28\textasciitilde30 Hz范围内的变化不显著改变脉动的时域结构。
\end{enumerate}

\subsubsection{频域信号与BPF分析}

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{figures/exp2_dynamic_frequency_domain.png}
    \caption{四组工况下 FFT 幅值谱（0\textasciitilde1500 Hz，探针1）}
    \label{fig:freq}
\end{figure}

\input{tables/table_bpf_analysis.tex}

图~\ref{fig:freq} 和表~\ref{tab:bpf_analysis} 给出了FFT分析结果。可以看出：
\begin{enumerate}
    \item 四组工况的频谱中均能清晰识别出 BPF 基频及其谐波（$2\times\text{BPF}$、$3\times\text{BPF}$）。实测峰值频率与理论 BPF 高度吻合，验证了传感器布置和信号采集的准确性。
    \item 近失速工况下 BPF 峰值幅值远大于全开工况，反映叶片通过时产生的压力扰动强度随压气机负荷增大而显著增强。
    \item 两种转速组合在频谱中均同时出现上下游两个转子的 BPF 成分（如30/35 Hz组的360 Hz和350 Hz），且R1（上游转子，12叶片）的 BPF 幅值普遍大于R2（下游转子，10叶片），可能是因为上游转子对下游进口流场仍具有显著的位势干涉效应。
    \item 在近失速工况的频谱中，除BPF谐波外还出现了一定的宽带噪声底板抬高现象，反映压气机在接近失速时流动中随机脉动成分的增加。
\end{enumerate}

\subsubsection{结果讨论}

\begin{enumerate}
    \item \textbf{节流状态对脉动强度的影响}：从全开（90\textdegree{}）到近失速（15\textdegree{}），压力脉动大幅增强。这是因为随着节流锥推进、流量减小，叶片负荷增大，叶尖泄漏流增强，叶片与端壁的相干作用加剧。
    \item \textbf{周向分布与相位关系}：三个周向布置的探针测得的时域信号波形高度一致，但在细节上存在相位差。相位差反映了压力扰动沿周向的传播特征，与转子的旋转方向一致。
    \item \textbf{对转布局的频率特征}：对转压气机中两排转子反向旋转，BPF频率分别为 $f_1 N_1$ 和 $f_2 N_2$。此外两排转子的相对运动还可能产生差频成分（$|f_1 N_1 - f_2 N_2|$），本实验中30/35 Hz组的差频为 $|360-350| = 10$ Hz，28/37 Hz组为 $|336-370| = 34$ Hz。
    \item \textbf{频谱分析的应用价值}：FFT分析可实时监测压气机运行状态。近失速时宽带噪声底板的抬高可作为失速预警的参考指标，而异常频率成分（如亚谐波）的出现则直接指示不稳定流动的发生。
\end{enumerate}

\subsection{思考题}

\begin{enumerate}
    \item \textbf{顶部端壁处压力脉动信号频率和什么因素有关？}\\
    答：主要与以下因素有关：（1）转子转速——BPF与转速成正比；（2）转子叶片数——BPF与叶片数成正比；（3）转子-静子（或转子-转子）干涉——对转布局下两排转子的相对运动产生差频与和频成分；（4）叶尖间隙——间隙大小影响泄漏流的强度和频率特征；（5）来流条件——来流湍流度和进气畸变影响脉动的幅度和频谱分布；（6）轴向位置——不同弦向位置对应不同的流动特征（前缘泄漏涡、中弦通道涡、尾缘脱落涡等）。

    \item \textbf{该压力脉动对压气机性能有什么影响？}\\
    答：压力脉动对压气机性能有以下影响：（1）损失增加——叶尖泄漏涡与端壁边界层的非定常相互作用是压气机效率损失的重要来源，强烈的压力脉动意味着更大的掺混损失和二次流损失；（2）噪声——BPF是压气机离散噪声的主要来源，压力脉动是噪声辐射的根源；（3）叶片振动——周期性的压力脉动作为激励力作用在叶片上，可能引起叶片强迫振动（共振），严重时导致高周疲劳（HCF）失效；（4）堵塞效应——叶尖泄漏流引起端壁区的流动堵塞，减小有效流通面积，降低压气机通流能力。

    \item \textbf{实验中如何保证测量的信号是真实的？}\\
    答：保证信号真实性的措施包括：（1）传感器频响——Kulite传感器的固有频率远高于BPF，保证能准确响应压力脉动；（2）Nyquist采样定理——采样频率5.12 kHz远大于最高关注频率（BPF的3倍谐波，约720 Hz），满足Shannon采样定理（$f_s > 2f_{\max}$）的要求；（3）抗混叠滤波——在A/D转换前设置低通滤波器，滤除高于$f_s/2$（2.56 kHz）的频率成分，避免频率混叠；（4）多次平均——对多次采集的数据进行平均，减小随机误差；（5）零点标定——实验前进行传感器零点采集和标定，消除系统误差；（6）信号完整性检查——观察时域波形是否存在削波、奇异点等异常，确保传感器和数据采集系统工作正常。
\end{enumerate}

\subsection{结论}

使用3支周向布置的Kulite动态压力传感器，在下游转子进口截面成功采集了四组工况（两种转速组合$\times$两种节流状态）下的非定常压力信号。FFT分析在四组工况的频谱中均清晰识别出上、下游转子各自的BPF及其谐波，实测峰值频率与理论BPF高度吻合，验证了$\text{BPF} = f_{\text{VFD}} \times N$的定量关系。实验结果表明，节流状态（流量）对压力脉动强度的影响远大于转速组合差异：近失速工况（15\textdegree{}）下的脉动RMS和峰峰值约为全开工况（90\textdegree{}）的2倍和5\textasciitilde7倍。掌握了动态压力信号的采集、FFT分析和BPF识别的基本方法。
